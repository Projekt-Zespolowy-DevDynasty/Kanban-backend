spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/newdatabase
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
  sql:
    init:
      mode: always